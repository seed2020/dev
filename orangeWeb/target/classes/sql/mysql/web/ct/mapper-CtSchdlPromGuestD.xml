<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : CT_SCHDL_PROM_GUEST_D[약속 참석자 상세] -->
<mapper namespace="com.innobiz.orange.web.ct.dao.CtSchdlPromGuestDDao">

	<resultMap id="ctSchdlPromGuestDMap" type="CtSchdlPromGuestDVo">
		<id property="schdlId" column="SCHDL_ID" /><!-- 일정ID -->
		<id property="guestUid" column="GUEST_UID" /><!-- 참석자UID -->
		<result property="guestNm" column="GUEST_NM" /><!-- 참석자 성명 -->
		<result property="guestPositNm" column="GUEST_POSIT_NM" /><!-- 참석자 직위명 -->
		<result property="guestDeptNm" column="GUEST_DEPT_NM" /><!-- 참석자 부서명 -->
		<result property="guestCompNm" column="GUEST_COMP_NM" /><!-- 참석자 회사명 -->
		<result property="statCd" column="STAT_CD" /><!-- 상태 코드 -->
		<result property="opinCont" column="OPIN_CONT" /><!-- 의견내용 -->
		<result property="guestEmplYn" column="GUEST_EMPL_YN" /><!-- 참석자_임직원 여부 -->
		<result property="email" column="EMAIL" /><!-- 이메일 -->
		<result property="emailSendYn" column="EMAIL_SEND_YN" /><!-- 이메일발송여부 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectCtSchdlPromGuestDWhere">
		<where>
			<if test="schdlId != null and schdlId !=''"> AND WPG_T.SCHDL_ID = #{schdlId, jdbcType=CHAR}</if>
			<if test="guestUid != null and guestUid !=''"> AND WPG_T.GUEST_UID = #{guestUid, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectCtSchdlPromGuestD" resultMap="ctSchdlPromGuestDMap" parameterType="CtSchdlPromGuestDVo" >
		/* com.innobiz.orange.web.ct.dao.CtSchdlPromGuestDDao.selectCtSchdlPromGuestD */
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			WPG_T.SCHDL_ID,
			WPG_T.GUEST_UID,
			WPG_T.GUEST_NM,
			WPG_T.GUEST_POSIT_NM,
			WPG_T.GUEST_DEPT_NM,
			WPG_T.GUEST_COMP_NM,
			WPG_T.STAT_CD,
			WPG_T.OPIN_CONT,
			WPG_T.GUEST_EMPL_YN,	
			WPG_T.EMAIL,
			WPG_T.EMAIL_SEND_YN,	
		</trim>
		FROM CT_SCHDL_PROM_GUEST_D WPG_T
		<include refid="selectCtSchdlPromGuestDWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY WPG_T.SCHDL_ID,WPG_T.GUEST_UID</if>
		<if test="pageNo != null and pageRowCnt != null">
		LIMIT #{pageStrt, jdbcType=NUMERIC}, #{pageRowCnt, jdbcType=NUMERIC}
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countCtSchdlPromGuestDR" resultType="Integer" parameterType="CtSchdlPromGuestDVo" >
		/* com.innobiz.orange.web.ct.dao.CtSchdlPromGuestDDao.countCtSchdlPromGuestDR */
		SELECT COUNT(*) CNT
		FROM CT_SCHDL_PROM_GUEST_D WPG_T
		<include refid="selectCtSchdlPromGuestDWhere"/>
	</select>
	
	<!-- 폴더 등록 저장 -->
	<insert id="insertCtSchdlPromGuestD" parameterType="CtSchdlPromGuestDVo" >
		/* com.innobiz.orange.web.ct.dao.CtSchdlPromGuestDDao.insertCtSchdlPromGuestD */
		INSERT INTO CT_SCHDL_PROM_GUEST_D
		<trim prefix="(" suffix=")" suffixOverrides=",">
			SCHDL_ID,
			GUEST_UID,
			GUEST_NM,
			GUEST_POSIT_NM,
			GUEST_DEPT_NM,
			GUEST_COMP_NM,
			STAT_CD,
			OPIN_CONT,
			GUEST_EMPL_YN,	
			EMAIL,
			EMAIL_SEND_YN,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{schdlId, jdbcType=VARCHAR},
			#{guestUid, jdbcType=VARCHAR},
			#{guestNm, jdbcType=NVARCHAR},
			#{guestPositNm, jdbcType=NVARCHAR},
			#{guestDeptNm, jdbcType=NVARCHAR},
			#{guestCompNm, jdbcType=NVARCHAR},
			#{statCd, jdbcType=VARCHAR},
			#{opinCont, jdbcType=NVARCHAR},
			#{guestEmplYn, jdbcType=VARCHAR},
			#{email, jdbcType=VARCHAR},
			#{emailSendYn, jdbcType=CHAR},
		
		</trim>
	</insert>
	
	<!-- 폴더 수정 -->
	<update id="updatePromGuestD" parameterType="CtSchdlPromGuestDVo" >
		/* com.innobiz.orange.web.ct.dao.CtSchdlPromGuestDDao.updatePromGuestD */
		UPDATE CT_SCHDL_PROM_GUEST_D
		<set>
			<if test="guestNm != null"> AGNT_ID = #{guestNm, jdbcType=NVARCHAR},</if>
			<if test="guestPositNm != null"> AGNT_ID = #{guestPositNm, jdbcType=NVARCHAR},</if>
			<if test="guestDeptNm != null"> AGNT_ID = #{guestDeptNm, jdbcType=NVARCHAR},</if>
			<if test="guestCompNm != null"> AGNT_ID = #{guestCompNm, jdbcType=NVARCHAR},</if>
			<if test="statCd != null"> AGNT_ID = #{statCd, jdbcType=VARCHAR},</if>
			<if test="opinCont != null"> AGNT_ID = #{opinCont, jdbcType=NVARCHAR},</if>
			<if test="guestEmplYn != null"> AGNT_ID = #{guestEmplYn, jdbcType=VARCHAR},</if>
			<if test="email != null"> AGNT_ID = #{email, jdbcType=VARCHAR},</if>
			<if test="emailSendYn != null"> AGNT_ID = #{emailSendYn, jdbcType=CHAR},</if>
			
		</set>
		<where>
			<if test="schdlId != null and schdlId != ''" > SCHDL_ID = #{schdlId, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 폴더 삭제 -->
	<delete id="deleteCtSchdlPromGuestD" parameterType="CtSchdlPromGuestDVo" >
		/* com.innobiz.orange.web.ct.dao.CtSchdlPromGuestDDao.deleteCtSchdlPromGuestD */
		DELETE FROM CT_SCHDL_PROM_GUEST_D
		<where>
			<if test="schdlId != null and schdlId != ''" > SCHDL_ID = #{schdlId, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>