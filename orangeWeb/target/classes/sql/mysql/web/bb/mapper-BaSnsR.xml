<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : BA_SNS_R[SNS관계] -->
<mapper namespace="com.innobiz.orange.web.bb.dao.BaSnsRDao">

	<resultMap id="baSnsRMap" type="BaSnsRVo">
		<id property="bullId" column="BULL_ID" /><!-- 게시물ID -->
		<id property="brdId" column="BRD_ID" /><!-- 게시판ID -->
		<id property="atrbId" column="ATRB_ID" /><!-- SNS구분 -->
		<result property="useYn" column="USE_YN" /><!-- 사용여부 -->
		<result property="sortOrdr" column="SORT_ORDR" /><!-- 정렬순서 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectBaSnsRWhere">
		<where>
			<if test="bullId != null and bullId !=''"> AND T.BULL_ID = #{bullId, jdbcType=VARCHAR}</if>
			<if test="brdId != null and brdId !=''"> AND T.BRD_ID = #{brdId, jdbcType=VARCHAR}</if>
			<if test="atrbId != null and atrbId !=''"> AND T.ATRB_ID = #{atrbId, jdbcType=VARCHAR}</if>
			<if test="useYn != null and useYn !=''"> AND T.USE_YN = #{useYn, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectBaSnsR" resultMap="baSnsRMap" parameterType="BaSnsRVo" >
		/* com.innobiz.orange.web.bb.dao.BaSnsRDao.selectBaSnsR */
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			T.BULL_ID,
			T.BRD_ID,
			T.ATRB_ID,
			T.USE_YN,
			T.SORT_ORDR,
		</trim>
		FROM BA_SNS_R T
		<include refid="selectBaSnsRWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.SORT_ORDR</if>
		<if test="pageNo != null and pageRowCnt != null">
		LIMIT #{pageStrt, jdbcType=NUMERIC}, #{pageRowCnt, jdbcType=NUMERIC}
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countBaSnsR" resultType="Integer" parameterType="BaSnsRVo" >
		/* com.innobiz.orange.web.bb.dao.BaSnsRDao.countBaSnsR */
		SELECT COUNT(*) CNT
		FROM BA_SNS_R T
		<include refid="selectBaSnsRWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertBaSnsR" parameterType="BaSnsRVo" >
		/* com.innobiz.orange.web.bb.dao.BaSnsRDao.insertBaSnsR */
		INSERT INTO BA_SNS_R
		<trim prefix="(" suffix=")" suffixOverrides=",">
			BULL_ID,
			BRD_ID,
			ATRB_ID,
			USE_YN,
			SORT_ORDR,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{bullId, jdbcType=VARCHAR},
			#{brdId, jdbcType=VARCHAR},
			#{atrbId, jdbcType=NVARCHAR},
			#{useYn, jdbcType=VARCHAR},
			#{sortOrdr, jdbcType=INTEGER},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateBaSnsR" parameterType="BaSnsRVo" >
		/* com.innobiz.orange.web.bb.dao.BaSnsRDao.updateBaSnsR */
		UPDATE BA_SNS_R
		<set>
			<if test="useYn != null"> USE_YN = #{useYn, jdbcType=VARCHAR},</if>
			<if test="sortOrdr != null"> SORT_ORDR = #{sortOrdr, jdbcType=INTEGER},</if>
		</set>
		<where>
			<if test="bullId != null and bullId !=''"> AND BULL_ID = #{bullId, jdbcType=VARCHAR}</if>
			<if test="brdId != null and brdId !=''"> AND BRD_ID = #{brdId, jdbcType=VARCHAR}</if>
			<if test="atrbId != null and atrbId !=''"> AND ATRB_ID = #{atrbId, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteBaSnsR" parameterType="BaSnsRVo" >
		/* com.innobiz.orange.web.bb.dao.BaSnsRDao.deleteBaSnsR */
		DELETE FROM BA_SNS_R
		<where>
			<if test="bullId != null and bullId !=''"> AND BULL_ID = #{bullId, jdbcType=VARCHAR}</if>
			<if test="brdId != null and brdId !=''"> AND BRD_ID = #{brdId, jdbcType=VARCHAR}</if>
			<if test="atrbId != null and atrbId !=''"> AND ATRB_ID = #{atrbId, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>