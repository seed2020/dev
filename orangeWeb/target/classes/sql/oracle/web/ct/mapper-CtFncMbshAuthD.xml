<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : CT_FNC_MBSH_AUTH_D [커뮤니티 기능 회원 권한 상세] -->
<mapper namespace="com.innobiz.orange.web.ct.dao.CtFncMbshAuthDDao">

	<resultMap id="ctFncMbshAuthDMap" type="CtFncMbshAuthDVo">
	    
	    <id property="compId" column="COMP_ID" /><!--회사 ID-->
	    <id property="ctFncUid" column="CT_FNC_UID" /><!--커뮤니티 기능 UID-->
	    <id property="seculCd" column="SECUL_CD" /><!--보안등급코드-->
		<result property="authCd" column="AUTH_CD" /><!--권한코드-->
		
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectCtFncMbshAuthDWhere">
		<where>
		    <if test="compId != null and compId != ''"> AND CFMAD_T.COMP_ID = #{compId, jdbcType=CHAR}</if>
		    <if test="ctFncUid != null and ctFncUid != ''"> AND CFMAD_T.CT_FNC_UID = #{ctFncUid, jdbcType=VARCHAR}</if>
		    <if test="seculCd != null and seculCd != ''"> AND CFMAD_T.SECUL_CD = #{seculCd, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectCtFncMbshAuthD" resultMap="ctFncMbshAuthDMap" parameterType="CtFncMbshAuthDVo" >
		/* com.innobiz.orange.web.ct.dao.CtFncMbshAuthDDao.selectCtFncMbshAuthD */
		
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			CFMAD_T.COMP_ID,
			CFMAD_T.CT_FNC_UID,
			CFMAD_T.SECUL_CD,
			CFMAD_T.AUTH_CD,
			
		</trim>
		FROM CT_FNC_MBSH_AUTH_D CFMAD_T
		<include refid="selectCtFncMbshAuthDWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY CFMAD_T.CT_FNC_UID</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	

	<!-- 목록조회 건수-->
	<select id="countCtFncMbshAuthD" resultType="Integer" parameterType="CtFncMbshAuthDVo" >
		/* com.innobiz.orange.web.ct.dao.CtFncMbshAuthDDao.countCtFncMbshAuthD */
		SELECT COUNT(*) CNT
		FROM CT_FNC_MBSH_AUTH_D CFMAD_T
		<include refid="selectCtFncMbshAuthDWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertCtFncMbshAuthD" parameterType="CtFncMbshAuthDVo" >
		/* com.innobiz.orange.web.ct.dao.CtFncMbshAuthDDao.insertCtFncMbshAuthD */
		INSERT INTO CT_FNC_MBSH_AUTH_D
		<trim prefix="(" suffix=")" suffixOverrides=",">
		    COMP_ID, CT_FNC_UID,
		    SECUL_CD, AUTH_CD,
		    

		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{compId, jdbcType=CHAR}, #{ctFncUid, jdbcType=VARCHAR},
			 #{seculCd, jdbcType=VARCHAR}, #{authCd, jdbcType=VARCHAR},
			
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateCtFncMbshAuthD" parameterType="CtFncMbshAuthDVo" >
		/* com.innobiz.orange.web.ct.dao.CtFncMbshAuthDDao.updateCtFncMbshAuthD */
		UPDATE CT_FNC_MBSH_AUTH_D
		<set>
			<if test="authCd != null"> AUTH_CD = #{authCd, jdbcType=VARCHAR},</if>
		</set>
		<where>
		    <if test="compId != null and compId != ''"> AND COMP_ID = #{compId, jdbcType=CHAR}</if>
		    <if test="ctFncUid != null and ctFncUid != ''"> AND CT_FNC_UID = #{ctFncUid, jdbcType=VARCHAR}</if>
		    <if test="seculCd != null and seculCd != ''"> AND SECUL_CD = #{seculCd, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteCtFncMbshAuthD" parameterType="CtFncMbshAuthDVo" >
		/* com.innobiz.orange.web.ct.dao.CtFncMbshAuthDDao.deleteCtFncMbshAuthD */
		DELETE FROM CT_FNC_MBSH_AUTH_D
		<where>
		    <if test="compId != null and compId != ''"> AND COMP_ID = #{compId, jdbcType=CHAR}</if>
		    <if test="ctFncUid != null and ctFncUid != ''"> AND CT_FNC_UID = #{ctFncUid, jdbcType=VARCHAR}</if>
		    <if test="seculCd != null and seculCd != ''"> AND SECUL_CD = #{seculCd, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>