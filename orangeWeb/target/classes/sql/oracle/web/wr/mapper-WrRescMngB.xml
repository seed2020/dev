<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WR_RESC_MNG_B[자원관리기본] -->
<mapper namespace="com.innobiz.orange.web.wr.dao.WrRescMngBDao">

	<resultMap id="wrRescMngBMap" type="WrRescMngBVo">
		<id property="rescMngId" column="RESC_MNG_ID" /><!-- 자원관리ID -->
		<result property="rescKndId" column="RESC_KND_ID" /><!-- 자원종류ID -->
		<result property="kndNm" column="KND_NM" /><!-- 종류명 -->
		<result property="compId" column="COMP_ID" /><!-- 회사ID -->
		<result property="rescId" column="RESC_ID" /><!-- 리소스ID -->
		<result property="rescNm" column="RESC_NM" /><!-- 자원명 -->
		<result property="rescLoc" column="RESC_LOC" /><!-- 자원위치 -->
		<result property="suplStat" column="SUPL_STAT" /><!-- 비품현황 -->
		<result property="rescAdmUid" column="RESC_ADM_UID" /><!-- 자원관리자UID -->
		<result property="rescAdmNm" column="RESC_ADM_NM" /><!-- 자원관리자명 -->
		<result property="discYn" column="DISC_YN" /><!-- 심의여부 -->
		<result property="useYn" column="USE_YN" /><!-- 사용여부 -->
		<result property="regrUid" column="REGR_UID" /><!-- 등록자UID -->
		<result property="regDt" column="REG_DT" /><!-- 등록일 -->
		<result property="regrNm" column="REGR_NM" /><!-- 등록자명 -->
		<result property="bgcolCd" column="BGCOL_CD" /><!-- 배경색코드 -->
	</resultMap>
			
	<!-- 조회조건 -->
	<sql id="selectWrRescMngBWhere">
		<where>
			<if test="rescMngId != null and rescMngId !=''"> AND T.RESC_MNG_ID = #{rescMngId, jdbcType=VARCHAR}</if>
			<if test="rescKndId != null and rescKndId !=''"> AND T.RESC_KND_ID = #{rescKndId, jdbcType=VARCHAR}</if>
			<if test="regrUid != null and regrUid !=''"> AND T.REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
			<if test="compId != null and compId !=''"> AND T.COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="discYn != null and discYn !=''"> AND T.DISC_YN = #{discYn, jdbcType=VARCHAR}</if>
			<if test="useYn != null and useYn !=''"> AND T.USE_YN = #{useYn, jdbcType=VARCHAR}</if>
			<if test="(schCat == null or schCat =='') and schWord != null and schWord != ''"> AND RESC_VA LIKE '%'||#{schWord, jdbcType=VARCHAR}||'%'</if>
			<if test="schCat == 'chkNm' and schWord != null and schWord != ''"> AND RESC_VA = #{schWord, jdbcType=VARCHAR}</if>
			<!-- 다중 조회조건(String) -->
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	<!-- 목록조회 -->
	<select id="selectWrRescMngB" resultMap="wrRescMngBMap" parameterType="WrRescMngBVo" >
		/* com.innobiz.orange.web.wr.dao.WrRescMngBDao.selectWrRescMngB */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			T.RESC_MNG_ID,
			T.RESC_KND_ID,
			T.COMP_ID,
			T.RESC_ID,
			T.RESC_LOC,
			T.SUPL_STAT,
			T.RESC_ADM_UID,
			T.DISC_YN,
			T.USE_YN,
			T.SORT_ORDR,
			T.REGR_UID,
			TO_CHAR(T.REG_DT, 'YYYY-MM-DD HH24:MI:SS') REG_DT, 
		    (SELECT RESC_VA FROM WR_RESC_B WHERE RESC_ID = T.RESC_ID AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko') ) AS RESC_NM ,
		    (SELECT RESC_VA FROM WR_RESC_B WHERE RESC_ID = (SELECT RESC_ID FROM WR_RESC_KND_B WHERE RESC_KND_ID = T.RESC_KND_ID) AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko') ) AS KND_NM ,
		    (SELECT RESC_VA FROM OR_RESC_B WHERE RESC_ID = (SELECT RESC_ID FROM OR_USER_B WHERE USER_UID = T.RESC_ADM_UID) AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko')) RESC_ADM_NM,
		    (SELECT RESC_VA FROM OR_RESC_B WHERE RESC_ID = (SELECT RESC_ID FROM OR_USER_B WHERE USER_UID = T.REGR_UID) AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko')) REGR_NM,
		    (SELECT BGCOL_CD FROM WR_RESC_KND_B WHERE RESC_KND_ID = T.RESC_KND_ID ) AS BGCOL_CD
		</trim>
		FROM WR_RESC_MNG_B T <if test="schWord != null and schWord != ''">INNER JOIN WR_RESC_B R ON R.RESC_ID = T.RESC_ID AND R.LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR}, 'ko')</if>
											INNER JOIN WR_RESC_KND_B K ON T.RESC_KND_ID = K.RESC_KND_ID
		<include refid="selectWrRescMngBWhere"/>
		<if test="orderBy != null"> ORDER BY ${orderBy}</if>
		<if test="orderBy == null"> ORDER BY K.SORT_ORDR, T.SORT_ORDR</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWrRescMngB" resultType="Integer" parameterType="WrRescMngBVo" >
		/* com.innobiz.orange.web.wr.dao.WrRescMngBDao.countWrRescMngB */
		SELECT COUNT(*) CNT
		FROM WR_RESC_MNG_B T <if test="schWord != null and schWord != ''">INNER JOIN WR_RESC_B R ON R.RESC_ID = T.RESC_ID AND R.LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR}, 'ko')</if>
											INNER JOIN WR_RESC_KND_B K ON T.RESC_KND_ID = K.RESC_KND_ID	
		<include refid="selectWrRescMngBWhere"/>
	</select>
		
	<!-- 등록 저장 -->
	<insert id="insertWrRescMngB" parameterType="WrRescMngBVo" >
		/* com.innobiz.orange.web.wr.dao.WrRescMngBDao.insertWrRescMngB */
		INSERT INTO WR_RESC_MNG_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			RESC_MNG_ID,
			RESC_KND_ID,
			COMP_ID,
			RESC_ID,
			RESC_NM,
			RESC_LOC,
			SUPL_STAT,
			RESC_ADM_UID,
			DISC_YN,
			USE_YN,
			SORT_ORDR,
			REGR_UID,
			REG_DT,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{rescMngId, jdbcType=VARCHAR},
			#{rescKndId, jdbcType=VARCHAR},
			#{compId, jdbcType=VARCHAR},
			#{rescId, jdbcType=VARCHAR},
			#{rescNm, jdbcType=NVARCHAR},
			#{rescLoc, jdbcType=VARCHAR},
			#{suplStat, jdbcType=VARCHAR},
			#{rescAdmUid, jdbcType=VARCHAR},
			#{discYn, jdbcType=VARCHAR},
			#{useYn, jdbcType=VARCHAR},
			#{sortOrdr, jdbcType=INTEGER},
			#{regrUid, jdbcType=VARCHAR},
			<if test="regDt == 'sysdate'">SYSDATE,</if><if test="regDt == null or regDt == ''"><if test="modDt == 'sysdate'">SYSDATE,</if><if test="modDt != 'sysdate'">NULL,</if></if>
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWrRescMngB" parameterType="WrRescMngBVo" >
		/* com.innobiz.orange.web.wr.dao.WrRescMngBDao.updateWrRescMngB */
		UPDATE WR_RESC_MNG_B
		<set>
			<if test="rescKndId != null"> RESC_KND_ID = #{rescKndId, jdbcType=VARCHAR},</if>
			<if test="rescNm != null"> RESC_NM = #{rescNm, jdbcType=NVARCHAR},</if>
			<if test="rescLoc != null"> RESC_LOC = #{rescLoc, jdbcType=VARCHAR},</if>
			<if test="suplStat != null"> SUPL_STAT = #{suplStat, jdbcType=VARCHAR},</if>
			<if test="rescAdmUid != null"> RESC_ADM_UID = #{rescAdmUid, jdbcType=VARCHAR},</if>
			<if test="discYn != null"> DISC_YN = #{discYn, jdbcType=VARCHAR},</if>
			<if test="useYn != null"> USE_YN = #{useYn, jdbcType=VARCHAR},</if>
			<if test="sortOrdr != null"> SORT_ORDR = #{sortOrdr, jdbcType=INTEGER},</if>
			<if test="modrUid != null"> MODR_UID = #{modrUid, jdbcType=VARCHAR},</if>
			<if test="modDt != null"> 
				<if test="modDt == 'sysdate'"> MOD_DT = SYSDATE,</if>
				<if test="modDt == ''"> MOD_DT = NULL,</if>
				<if test="modDt != '' and modDt != 'sysdate'"> MOD_DT = TO_DATE(#{modDt, jdbcType=VARCHAR}, 'YYYY-MM-DD HH24:MI:SS'),</if>
			</if>
		</set>
		<where>
			<if test="rescMngId != null and rescMngId !=''"> AND RESC_MNG_ID = #{rescMngId, jdbcType=VARCHAR}</if>			
			<if test="regrUid != null and regrUid !=''"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWrRescMngB" parameterType="WrRescMngBVo" >
		/* com.innobiz.orange.web.wr.dao.WrRescMngBDao.deleteWrRescMngB */
		DELETE FROM WR_RESC_MNG_B
		<where>
			<if test="rescMngId != null and rescMngId !=''"> AND RESC_MNG_ID = #{rescMngId, jdbcType=VARCHAR}</if>			
			<if test="regrUid != null and regrUid !=''"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
		</where>
	</delete>
	
	<!-- 순서 조회-->
	<select id="maxWrRescMngB" resultType="Integer" parameterType="WrRescMngBVo" >
		/* com.innobiz.orange.web.wr.dao.WrRescMngBDao.maxWrRescMngB */
		SELECT NVL(MAX(SORT_ORDR),0)+1 AS CNT
		FROM WR_RESC_MNG_B T
		<where>
		<if test="compId != null"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
		<if test="rescKndId != null"> AND RESC_KND_ID = #{rescKndId, jdbcType=VARCHAR}</if>		
		</where>
	</select>

</mapper>