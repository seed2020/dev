<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WH_CAT_GRP_L[유형목록] -->
<mapper namespace="com.innobiz.orange.web.wh.dao.WhCatGrpLDao">

	<resultMap id="whCatGrpLMap" type="WhCatGrpLVo">
		<id property="catNo" column="CAT_NO" /><!-- 유형번호 -->
		<result property="catGrpId" column="CAT_GRP_ID" /><!-- 유형그룹ID -->
		<result property="catNm" column="CAT_NM" /><!--유형명 -->
		<result property="rescId" column="RESC_ID" /><!-- 리소스ID -->
		<result property="sortOrdr" column="SORT_ORDR" /><!-- 정렬순서 -->
	</resultMap>
      
	<!-- 조회조건 -->
	<sql id="selectWhCatGrpLWhere">
		<where>
			<if test="catNo != null and catNo !=''"> AND T.CAT_NO = #{catNo, jdbcType=VARCHAR}</if>
			<if test="catGrpId != null and catGrpId !=''"> AND T.CAT_GRP_ID = #{catGrpId, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWhCatGrpL" resultMap="whCatGrpLMap" parameterType="WhCatGrpLVo" >
		/* com.innobiz.orange.web.wh.dao.WhCatGrpLDao.selectWhCatGrpL */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			T.CAT_NO,
	        T.CAT_GRP_ID,
	        (SELECT RESC_VA FROM WH_RESC_B WHERE RESC_ID = T.RESC_ID AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko')) AS CAT_NM,
	        T.RESC_ID,
	        T.SORT_ORDR,
		</trim>
		FROM WH_CAT_GRP_L T
		<include refid="selectWhCatGrpLWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.SORT_ORDR ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWhCatGrpL" resultType="Integer" parameterType="WhCatGrpLVo" >
		/* com.innobiz.orange.web.wh.dao.WhCatGrpLDao.countWhCatGrpL */
		SELECT COUNT(*) CNT
		FROM WH_CAT_GRP_L T
		<include refid="selectWhCatGrpLWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWhCatGrpL" parameterType="WhCatGrpLVo" >
		/* com.innobiz.orange.web.wh.dao.WhCatGrpLDao.insertWhCatGrpL */
		INSERT INTO WH_CAT_GRP_L
		<trim prefix="(" suffix=")" suffixOverrides=",">
			CAT_NO,
	        CAT_GRP_ID,
	        CAT_NM,
	        RESC_ID,
	        SORT_ORDR,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{catNo, jdbcType=INTEGER},
			#{catGrpId, jdbcType=VARCHAR},
			#{catNm, jdbcType=VARCHAR},
			#{rescId, jdbcType=VARCHAR},
			#{sortOrdr, jdbcType=INTEGER},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWhCatGrpL" parameterType="WhCatGrpLVo" >
		/* com.innobiz.orange.web.wh.dao.WhCatGrpLDao.updateWhCatGrpL */
		UPDATE WH_CAT_GRP_L
		<set>
			<if test="catNm != null"> CAT_NM = #{catNm, jdbcType=VARCHAR},</if>
			<if test="sortOrdr != null"> SORT_ORDR = #{sortOrdr, jdbcType=INTEGER},</if>
		</set>
		<where>
			<if test="catNo != null and catNo !=''"> AND CAT_NO = #{catNo, jdbcType=VARCHAR}</if>
			<if test="catGrpId != null and catGrpId !=''"> AND CAT_GRP_ID = #{catGrpId, jdbcType=VARCHAR}</if>			
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWhCatGrpL" parameterType="WhCatGrpLVo" >
		/* com.innobiz.orange.web.wh.dao.WhCatGrpLDao.deleteWhCatGrpL */
		DELETE FROM WH_CAT_GRP_L
		<where>
			<if test="catNo != null and catNo !=''"> AND CAT_NO = #{catNo, jdbcType=VARCHAR}</if>
			<if test="catGrpId != null and catGrpId !=''"> AND CAT_GRP_ID = #{catGrpId, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>