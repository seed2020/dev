<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : DM_STOR_B[저장소기본] -->
<mapper namespace="com.innobiz.orange.web.dm.dao.DmStorBDao">

	<resultMap id="dmStorBMap" type="DmStorBVo">
		<id property="storId" column="STOR_ID" /><!-- 저장소ID	 -->
		<result property="compId" column="COMP_ID" /><!-- 회사ID -->
		<result property="tblNm" column="TBL_NM" /><!-- 테이블명 -->
		<result property="storNm" column="STOR_NM" /><!-- 저장소명 -->
		<result property="rescId" column="RESC_ID" /><!-- 리소스ID -->
		<result property="useYn" column="USE_YN" /><!-- 사용여부 -->
		<result property="dftYn" column="DFT_YN" /><!-- 기본여부 -->
		<result property="regrUid" column="REGR_UID" /><!-- 등록자 -->
		<result property="regDt" column="REG_DT" /><!-- 등록일시 -->
		<result property="regrNm" column="REGR_NM" /><!-- 등록자명 -->
		<result property="modrUid" column="MODR_UID" /><!-- 수정자 -->
		<result property="modDt" column="MOD_DT" /><!-- 수정일시 -->
	</resultMap>
	
	
	<!-- 조회조건 -->
	<sql id="selectDmStorBWhere">
		<where>
			<if test="compId != null and compId !=''"> AND T.COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="regrUid != null and regrUid !=''"> AND T.REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
			<if test="storId != null and storId !=''"> AND T.STOR_ID = #{storId, jdbcType=VARCHAR}</if>
			<if test="'storNm'.equals(schCat) and schWord != null and schWord != ''">AND R.LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko') AND R.RESC_ID = T.RESC_ID AND R.RESC_VA LIKE '%'||#{schWord, jdbcType=VARCHAR}||'%'</if>
			<if test="'tblNm'.equals(schCat) and schWord != null and schWord != ''">AND T.TBL_NM = #{schWord, jdbcType=VARCHAR}</if>
			<if test="useYn != null and useYn !=''"> AND T.USE_YN = #{useYn, jdbcType=VARCHAR}</if>
			<if test="dftYn != null and dftYn !=''"> AND T.DFT_YN = #{dftYn, jdbcType=VARCHAR}</if>
			<!-- 다중 조회조건(String) -->
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectDmStorB" resultMap="dmStorBMap" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.selectDmStorB */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			T.STOR_ID,
			T.COMP_ID,
			T.TBL_NM,
			T.RESC_ID,
			T.USE_YN,
			T.DFT_YN,
			T.REGR_UID,
			T.REG_DT,
			T.MODR_UID,
			T.MOD_DT,
			(SELECT RESC_VA FROM OR_RESC_B OR_T WHERE RESC_ID = T.RESC_ID  AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko')) AS REGR_NM ,
			(SELECT RESC_VA FROM DM_RESC_B WHERE STOR_ID = T.STOR_ID AND RESC_ID = T.RESC_ID AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko')) AS STOR_NM ,
		</trim>
		FROM DM_STOR_B T
		<if test="'storNm'.equals(schCat) and schWord != null and schWord != ''">, DM_RESC_B R </if>
		<include refid="selectDmStorBWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.REG_DT DESC </if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countDmStorB" resultType="Integer" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.countDmStorB */
		SELECT COUNT(*) CNT
		FROM DM_STOR_B T
		<if test="'storNm'.equals(schCat) and schWord != null and schWord != ''">, DM_RESC_B R </if>
		<include refid="selectDmStorBWhere"/>
	</select>
		
	<!-- 등록 저장 -->
	<insert id="insertDmStorB" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.insertDmStorB */
		INSERT INTO DM_STOR_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			STOR_ID,
			COMP_ID,
			TBL_NM,
			STOR_NM,
			RESC_ID,
			USE_YN,
			DFT_YN,
			REGR_UID,
			REG_DT,
			MODR_UID,
			MOD_DT,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{storId, jdbcType=VARCHAR},
			#{compId, jdbcType=VARCHAR},
			#{tblNm, jdbcType=NVARCHAR},
			#{storNm, jdbcType=NVARCHAR},
			#{rescId, jdbcType=VARCHAR},
			#{useYn, jdbcType=NVARCHAR},
			#{dftYn, jdbcType=VARCHAR},
			<if test="regrUid != null">#{regrUid, jdbcType=VARCHAR},</if><if test="regrUid == null">NULL,</if>
			<if test="regDt == 'sysdate'">SYSDATE,</if><if test="regDt == null or regDt == ''">NULL,</if>
			<if test="regDt != null and regDt != '' and regDt != 'sysdate'">TO_DATE(#{regDt, jdbcType=VARCHAR}, 'YYYY-MM-DD HH24:MI:SS'),</if>
			<if test="modrUid != null">#{modrUid, jdbcType=VARCHAR},</if><if test="modrUid == null">NULL,</if>
			<if test="modDt == 'sysdate'">SYSDATE,</if><if test="modDt == null or modDt == ''">NULL,</if>
			<if test="modDt != null and modDt != '' and modDt != 'sysdate'">TO_DATE(#{modDt, jdbcType=VARCHAR}, 'YYYY-MM-DD HH24:MI:SS'),</if>
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateDmStorB" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.updateDmStorB */
		UPDATE DM_STOR_B
		<set>
			<if test="tblNm != null"> TBL_NM = #{tblNm, jdbcType=VARCHAR},</if>
			<if test="storNm != null"> STOR_NM = #{storNm, jdbcType=NVARCHAR},</if>
			<if test="rescId != null"> RESC_ID = #{rescId, jdbcType=VARCHAR},</if>
			<if test="useYn != null"> USE_YN = #{useYn, jdbcType=VARCHAR},</if>
			<if test="dftYn != null"> DFT_YN = #{dftYn, jdbcType=VARCHAR},</if>
			<if test="modrUid != null"> MODR_UID = #{modrUid, jdbcType=VARCHAR},</if>
			<if test="modDt != null"> 
				<if test="modDt == 'sysdate'"> MOD_DT = SYSDATE,</if>
				<if test="modDt == ''"> MOD_DT = NULL,</if>
				<if test="modDt != '' and modDt != 'sysdate'"> MOD_DT = TO_DATE(#{modDt, jdbcType=VARCHAR}, 'YYYY-MM-DD HH24:MI:SS'),</if>
			</if>
		</set>
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="storId != null and storId !=''"> AND STOR_ID = #{storId, jdbcType=VARCHAR}</if>
			<if test="modrUid != null"> AND REGR_UID = #{modrUid, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteDmStorB" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.deleteDmStorB */
		DELETE FROM DM_STOR_B
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="storId != null and storId !=''"> AND STOR_ID = #{storId, jdbcType=VARCHAR}</if>
			<if test="regrUid != null"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
		</where>
	</delete>
	
	<!-- 테이블 생성 [문서목록]-->
	<update id="createDmDocL" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmStorL */
		CREATE TABLE ${tblNm} (
			DOC_ID NUMBER,
			DOC_GRP_ID NUMBER,			
			DFT_YN CHAR(1),
		<foreach item="colmVo" index="index" collection="colmVoList">
			${colmVo.itemNm} ${colmVo.dataTyp} <if test="colmVo.colmLen != null and colmVo.colmLen != ''">(${colmVo.colmLen})</if>,
		</foreach>
			CONSTRAINT PK_${tblNm} PRIMARY KEY (DOC_ID)<!-- ,
			INDEX IDX_${tblNm} ON ${tblNm}(DOC_GRP_ID,DFT_YN,SUBJ,REGR_UID) -->
		)
	</update>
	
	<!-- 인덱스 생성 [문서목록]-->
	<update id="createDmDocLIdx" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmStorLIdx */
		CREATE INDEX IDX_${tblNm} ON ${tblNm}(DOC_GRP_ID,DFT_YN,SUBJ,REGR_UID,MODR_UID,DOC_ID)
	</update>
	
	<!-- 테이블 생성 [문서상세]-->
	<update id="createDmDocD" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmDocD */
		CREATE TABLE ${tblNm} (
			DOC_GRP_ID NUMBER NOT NULL,
			SUB_YN CHAR(1) NOT NULL,
			SUB_DOC_GRP_ID NUMBER,
			DOC_PID NUMBER,
			SORT_ORDR INT NOT NULL,
			SORT_DPTH INT NOT NULL,
			STAT_CD VARCHAR2(30) NOT NULL,
			FLD_ID VARCHAR2(30) NOT NULL,
			DOC_NO VARCHAR2(400),
			KEEP_PRD_DT DATE,
			OWNR_UID VARCHAR2(30) NOT NULL,
			DOC_KEEP_PRD_CD VARCHAR2(30),
			SECUL_CD VARCHAR2(30) NOT NULL,
			CMPL_DT DATE,
			READ_CNT INT,
			REF_TYP CHAR(2),
			REF_ID VARCHAR2(30),
			REF_URL VARCHAR2(1000),
			ORGN_YN CHAR(1),
			CONSTRAINT PK_${tblNm} PRIMARY KEY (DOC_GRP_ID)<!-- ,
			INDEX IDX_${tblNm} ON ${tblNm}(SUB_YN,STAT_CD,DOC_NO,OWNR_UID,DOC_KEEP_PRD_CD,SECUL_CD) -->
		)
	</update>
	
	<!-- 인덱스 생성 [문서상세]-->
	<update id="createDmDocDIdx" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmDocDIdx */
		CREATE INDEX IDX_${tblNm} ON ${tblNm}(SUB_YN,STAT_CD,FLD_ID,DOC_NO,OWNR_UID,DOC_KEEP_PRD_CD,SECUL_CD,DOC_GRP_ID)
	</update>
	
	<!-- 테이블 생성 [분류관계]-->
	<update id="createDmClsR" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmClsR */
		CREATE TABLE ${tblNm} (
			DOC_GRP_ID NUMBER,
			CLS_ID VARCHAR2(30),
			CONSTRAINT PK_${tblNm} PRIMARY KEY (DOC_GRP_ID,CLS_ID)
		)
	</update>
	
	<!-- 테이블 생성 [버전목록]-->
	<update id="createDmDocVerL" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmDocVerL */
		CREATE TABLE ${tblNm} (
			DOC_GRP_ID NUMBER NOT NULL,
			DOC_ID NUMBER NOT NULL,
			VER_VA VARCHAR2(5) NULL,
			CONSTRAINT PK_${tblNm} PRIMARY KEY (DOC_GRP_ID,DOC_ID)
		)
	</update>
	
	<!-- 테이블 생성 [키워드]-->
	<update id="createDmKwdL" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmKwdL */
		CREATE TABLE ${tblNm} (
			DOC_GRP_ID NUMBER NOT NULL,
			KWD_NM VARCHAR2(120) NOT NULL,
			CONSTRAINT PK_${tblNm} PRIMARY KEY (DOC_GRP_ID,KWD_NM)
		)
	</update>
	
	<!-- 테이블 생성 [첨부파일]-->
	<update id="createDmFileD" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmFileD */
		CREATE TABLE ${tblNm} (
			FILE_ID INT NOT NULL,
			DM_REF_ID INT NULL,
			DISP_NM VARCHAR2(240) NULL,
			DISP_ORDR INT NULL,
			FILE_EXT VARCHAR2(30) NULL,
			FILE_SIZE INT NULL,
			SAVE_PATH VARCHAR2(240) NULL,
			USE_YN CHAR(1) NULL,
			REGR_UID VARCHAR2(30) NULL,
			REG_DT DATE NULL,
			CONSTRAINT PK_${tblNm} PRIMARY KEY (FILE_ID)
		)
	</update>
	
	<!-- 테이블 생성 [작업이력]-->
	<update id="createDmTaskH" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.createDmTaskH */
		CREATE TABLE ${tblNm} (
			DOC_GRP_ID NUMBER NOT NULL,
			TASK_DT DATE NOT NULL,
			USER_UID VARCHAR2(30) NOT NULL,
			TASK_CD VARCHAR2(30) NOT NULL,
			NOTE VARCHAR2(2000),
			CONSTRAINT PK_${tblNm} PRIMARY KEY (DOC_GRP_ID, TASK_DT, USER_UID, TASK_CD)
		)
	</update>
	
	<!-- 테이블 삭제 -->
	<update id="dropDmStorL" parameterType="DmStorBVo" >
		/* com.innobiz.orange.web.dm.dao.DmStorBDao.dropDmStorL */
		DROP TABLE ${tblNm} CASCADE CONSTRAINT PURGE
	</update>
	
</mapper>