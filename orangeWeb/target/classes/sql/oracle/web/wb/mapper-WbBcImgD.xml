<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WB_BC_IMG_D[명함지정인관계] -->
<mapper namespace="com.innobiz.orange.web.wb.dao.WbBcImgDDao">

	<resultMap id="wbBcImgDMap" type="WbBcImgDVo">
		<id property="bcId" column="BC_ID" /><!-- 명함ID -->
		<result property="imgPath" column="IMG_PATH" /><!-- 이미지경로 -->
		<result property="imgWdth" column="IMG_WDTH" /><!-- 이미지넓이 -->
		<result property="imgHght" column="IMG_HGHT" /><!-- 이미지높이 -->
		<result property="modrUid" column="MODR_UID" /><!-- 수정자UID -->
		<result property="modDt" column="MOD_DT" /><!-- 수정일시 -->
		<result property="rnum" column="RNUM" /><!-- 레코드 번호 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectWbBcImgDWhere">
		<where>
			<if test="bcId != null and bcId !=''"> AND WBI_T.BC_ID = #{bcId, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWbBcImgD" resultMap="wbBcImgDMap" parameterType="WbBcImgDVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcImgDDao.selectWbBcImgD */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			WBI_T.BC_ID,
			WBI_T.IMG_PATH,
			WBI_T.IMG_WDTH,
			WBI_T.IMG_HGHT,
			WBI_T.MODR_UID,
			TO_CHAR(WBI_T.MOD_DT, 'YYYY-MM-DD HH24:MI:SS') MOD_DT,
		</trim>
		FROM WB_BC_IMG_D WBI_T
		<include refid="selectWbBcImgDWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY WBI_T.BC_ID ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWbBcImgD" resultType="Integer" parameterType="WbBcImgDVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcImgDDao.countWbBcImgD */
		SELECT COUNT(*) CNT
		FROM WB_BC_IMG_D WBI_T
		<include refid="selectWbBcImgDWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWbBcImgD" parameterType="WbBcImgDVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcImgDDao.insertWbBcImgD */
		INSERT INTO WB_BC_IMG_D
		<trim prefix="(" suffix=")" suffixOverrides=",">
			BC_ID , IMG_PATH,	IMG_WDTH, IMG_HGHT,	MODR_UID, 
			MOD_DT,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{bcId, jdbcType=VARCHAR}, #{imgPath, jdbcType=VARCHAR},	#{imgWdth, jdbcType=INTEGER},	#{imgHght, jdbcType=INTEGER},	#{modrUid, jdbcType=VARCHAR},
			<if test="modDt == 'sysdate'">SYSDATE,</if><if test="modDt == null or modDt == ''">NULL,</if>
			<if test="modDt != null and modDt != '' and modDt != 'sysdate'">TO_DATE(#{modDt, jdbcType=VARCHAR}, 'YYYY-MM-DD HH24:MI:SS'),</if>
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWbBcImgD" parameterType="WbBcImgDVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcImgDDao.updateWbBcImgD */
		UPDATE WB_BC_IMG_D
		<set>
			<if test="imgPath != null"> IMG_PATH = #{imgPath, jdbcType=VARCHAR},</if>
			<if test="imgWdth != null"> IMG_WDTH = #{imgWdth, jdbcType=INTEGER},</if>
			<if test="imgHght != null"> IMG_HGHT = #{imgHght, jdbcType=INTEGER},</if>
			<if test="modrUid != null"> MODR_UID = #{modrUid, jdbcType=VARCHAR},</if>
			<if test="modDt != null"> 
				<if test="modDt == 'sysdate'"> MOD_DT = SYSDATE,</if>
				<if test="modDt == ''"> MOD_DT = NULL,</if>
				<if test="modDt != '' and modDt != 'sysdate'"> MOD_DT = TO_DATE(#{modDt, jdbcType=VARCHAR}, 'YYYY-MM-DD HH24:MI:SS'),</if>
			</if>
		</set>
		<where>
			<if test="bcId != null and bcId !=''"> AND BC_ID = #{bcId, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWbBcImgD" parameterType="WbBcImgDVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcImgDDao.deleteWbBcImgD */
		DELETE FROM WB_BC_IMG_D
		<where>
			<if test="bcId != null and bcId !=''"> AND BC_ID = #{bcId, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</delete>

</mapper>