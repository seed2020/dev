<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WB_BC_LST_SETUP_B[명함목록설정기본] -->
<mapper namespace="com.innobiz.orange.web.wb.dao.WbBcLstSetupBDao">

	<resultMap id="wbBcLstSetupBMap" type="WbBcLstSetupBVo">
		<id property="compId" column="COMP_ID" /><!-- 회사ID -->
		<id property="atrbId" column="ATRB_ID" /><!-- 속성ID -->
		<result property="msgId" column="MSG_ID" /><!-- 메시지ID -->
		<result property="sortOrdr" column="SORT_ORDR" /><!-- 정렬순서 -->
		<result property="dftSetupYn" column="DFT_SETUP_YN" /><!-- 기본설정여부 -->
		<result property="alnVa" column="ALN_VA" /><!-- 줄마춤값 -->
		<result property="wdthPerc" column="WDTH_PERC" /><!-- 넓이퍼센트 -->
		<result property="dispYn" column="DISP_YN" /><!-- 표시여부 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectWbBcLstSetupBWhere">
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="atrbId != null and atrbId !=''"> AND ATRB_ID = #{atrbId, jdbcType=VARCHAR}</if>
			<if test="dftSetupYn != null and dftSetupYn !=''"> AND DFT_SETUP_YN = #{dftSetupYn, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWbBcLstSetupB" resultMap="wbBcLstSetupBMap" parameterType="WbBcLstSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcLstSetupBDao.selectWbBcLstSetupB */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			COMP_ID , ATRB_ID , MSG_ID , SORT_ORDR , DFT_SETUP_YN , ALN_VA , WDTH_PERC , DISP_YN , 
		</trim>
		FROM WB_BC_LST_SETUP_B WBLS_T  
		<include refid="selectWbBcLstSetupBWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY WBLS_T.SORT_ORDR ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWbBcLstSetupB" resultType="Integer" parameterType="WbBcLstSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcLstSetupBDao.countWbBcLstSetupB */
		SELECT COUNT(*) CNT
		FROM WB_BC_LST_SETUP_B WBLS_T 
		<include refid="selectWbBcLstSetupBWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWbBcLstSetupB" parameterType="WbBcLstSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcLstSetupBDao.insertWbBcLstSetupB */
		INSERT INTO WB_BC_LST_SETUP_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			COMP_ID , ATRB_ID , MSG_ID , SORT_ORDR , DFT_SETUP_YN , 
			ALN_VA , WDTH_PERC , DISP_YN , 
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{compId, jdbcType=VARCHAR},#{atrbId, jdbcType=VARCHAR},#{msgId, jdbcType=VARCHAR},#{sortOrdr, jdbcType=INTEGER},#{dftSetupYn, jdbcType=CHAR},
			#{alnVa, jdbcType=VARCHAR},#{wdthPerc, jdbcType=VARCHAR},#{dispYn, jdbcType=CHAR},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWbBcLstSetupB" parameterType="WbBcLstSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcLstSetupBDao.updateWbBcLstSetupB */
		UPDATE WB_BC_LST_SETUP_B
		<set>
			<if test="sortOrdr != null"> SORT_ORDR = #{sortOrdr, jdbcType=INTEGER},</if>
			<if test="dftSetupYn != null"> DFT_SETUP_YN = #{dftSetupYn, jdbcType=VARCHAR},</if>
			<if test="alnVa != null"> ALN_VA = #{alnVa, jdbcType=INTEGER},</if>
			<if test="wdthPerc != null"> WDTH_PERC = #{wdthPerc, jdbcType=INTEGER},</if>
			<if test="dispYn != null"> DISP_YN = #{dispYn, jdbcType=INTEGER},</if>
		</set>
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="atrbId != null and atrbId !=''"> AND ATRB_ID = #{atrbId, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWbBcLstSetupB" parameterType="WbBcLstSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcLstSetupBDao.deleteWbBcLstSetupB */
		DELETE FROM WB_BC_LST_SETUP_B
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="atrbId != null and atrbId !=''"> AND ATRB_ID = #{atrbId, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>