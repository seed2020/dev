<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WB_BC_APNTR_R[명함지정인관계] -->
<mapper namespace="com.innobiz.orange.web.wb.dao.WbBcApntrRDao">

	<resultMap id="wbBcApntrRMap" type="WbBcApntrRVo">
		<result property="bcId" column="BC_ID" /><!-- 명함ID -->
		<result property="bcApntrUid" column="BC_APNTR_UID" /><!-- 명함지정인UID -->
		<result property="userNm" column="USER_NM" /><!-- 지정인명 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectWbBcApntrRWhere">
		<where>
			<if test="bcId != null and bcId !=''"> AND WBA_T.BC_ID = #{bcId, jdbcType=VARCHAR}</if>
			<if test="bcApntrUid != null"> AND WBA_T.BC_APNTR_UID = #{bcApntrUid, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWbBcApntrR" resultMap="wbBcApntrRMap" parameterType="WbBcApntrRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcApntrRDao.selectWbBcApntrR */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			BC_ID , BC_APNTR_UID , OU_T.USER_NM , 
		</trim>
		FROM WB_BC_APNTR_R WBA_T INNER JOIN OR_USER_B OU_T ON OU_T.USER_UID = WBA_T.BC_APNTR_UID
		<include refid="selectWbBcApntrRWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY OU_T.USER_NM ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWbBcApntrR" resultType="Integer" parameterType="WbBcApntrRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcApntrRDao.countWbBcApntrR */
		SELECT COUNT(*) CNT
		FROM WB_BC_APNTR_R WBA_T INNER JOIN OR_USER_B OU_T ON OU_T.USER_UID = WBA_T.BC_APNTR_UID
		<include refid="selectWbBcApntrRWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWbBcApntrR" parameterType="WbBcApntrRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcApntrRDao.insertWbBcApntrR */
		INSERT INTO WB_BC_APNTR_R
		<trim prefix="(" suffix=")" suffixOverrides=",">
			BC_ID , BC_APNTR_UID,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{bcId, jdbcType=VARCHAR},#{bcApntrUid, jdbcType=VARCHAR},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWbBcApntrR" parameterType="WbBcApntrRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcApntrRDao.updateWbBcApntrR */
		UPDATE WB_BC_APNTR_R
		<set>
			<if test="cntcTypCd != null"> CNTC_TYP_CD = #{cntcTypCd, jdbcType=VARCHAR},</if>
			<if test="cntcCont != null"> CNTC_CONT = #{cntcCont, jdbcType=VARCHAR},</if>
			<if test="dftCntcYn != null"> DFT_CNTC_YN = #{dftCntcYn, jdbcType=VARCHAR},</if>
		</set>
		<where>
			<if test="bcId != null and bcId !=''"> AND BC_ID = #{bcId, jdbcType=VARCHAR}</if>
			<if test="bcCntcSeq != null"> AND BC_CNTC_SEQ = #{bcCntcSeq, jdbcType=INTEGER}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWbBcApntrR" parameterType="WbBcApntrRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcApntrRDao.deleteWbBcApntrR */
		DELETE FROM WB_BC_APNTR_R
		<where>
			<if test="bcId != null and bcId !=''"> AND BC_ID = #{bcId, jdbcType=VARCHAR}</if>
			<if test="deleteUids != null"> AND BC_APNTR_UID NOT IN ( <foreach collection="deleteUids" item="item" index="index" separator=",">#{item, jdbcType=VARCHAR}</foreach> )</if>
		</where>
	</delete>

</mapper>