<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WC_SCHDL_DEPT_R[업무일지] -->
<mapper namespace="com.innobiz.orange.web.wc.dao.WcSchdlDeptRDao">

	<resultMap id="wcSchdlDeptRMap" type="WcSchdlDeptRVo">
		<id property="schdlId" column="SCHDL_ID" /><!-- 일정ID -->
		<id property="orgId" column="ORG_ID" /><!-- 조직ID -->
		<id property="orgNm" column="ORG_NM" /><!-- 조직명 -->		
	</resultMap>
			
	<sql id="selectWcSchdlDeptRWhere">
		<where>
			<if test="schdlId != null"> AND T.SCHDL_ID = #{schdlId, jdbcType=INTEGER}</if>
			<if test="orgId != null"> AND T.ORG_ID = #{orgId, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<select id="selectWcSchdlDeptR" resultMap="wcSchdlDeptRMap" parameterType="WcSchdlDeptRVo" >
		/* com.innobiz.orange.web.wc.dao.WcSchdlDeptRDao.selectWcSchdlDeptR */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			T.SCHDL_ID,
			T.ORG_ID,
			(SELECT RESC_VA FROM OR_RESC_B WHERE RESC_ID = (SELECT RESC_ID FROM OR_ORG_B WHERE ORG_ID = T.ORG_ID) AND LANG_TYP_CD = NVL(#{queryLang, jdbcType=VARCHAR},'ko') ) AS ORG_NM,
		</trim>
		FROM WC_SCHDL_DEPT_R T 
		<include refid="selectWcSchdlDeptRWhere" />
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.SCHDL_ID ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>

	<select id="countWcSchdlDeptR" resultType="Integer" parameterType="WcSchdlDeptRVo" >
		/* com.innobiz.orange.web.wc.dao.WcSchdlDeptRDao.countWcSchdlDeptR */
		SELECT COUNT(*) CNT
		FROM WC_SCHDL_DEPT_R T
		<include refid="selectWcSchdlDeptRWhere" />
	</select>

	<insert id="insertWcSchdlDeptR" parameterType="WcSchdlDeptRVo">
		/* com.innobiz.orange.web.wc.dao.WcSchdlDeptRDao.insertWcSchdlDeptR */
		INSERT INTO WC_SCHDL_DEPT_R
		<trim prefix="(" suffix=")" suffixOverrides=",">
			SCHDL_ID,
			ORG_ID,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides=",">
			#{schdlId, jdbcType=INTEGER},
			#{orgId, jdbcType=VARCHAR},
		</trim>
	</insert>

	<delete id="deleteWcSchdlDeptR" parameterType="WcSchdlDeptRVo">
		/* com.innobiz.orange.web.wc.dao.WcSchdlDeptRDao.deleteWcSchdlDeptR */
		DELETE FROM WC_SCHDL_DEPT_R
		<where>
			<if test="schdlId != null and schdlId !=''"> AND SCHDL_ID = #{schdlId, jdbcType=INTEGER}</if>
			<if test="orgId != null and orgId !=''"> AND ORG_ID = #{orgId, jdbcType=VARCHAR}</if>
		</where>
	</delete>
	
</mapper>