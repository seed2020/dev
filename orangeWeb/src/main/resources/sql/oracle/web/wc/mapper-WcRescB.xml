<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : PT_AUTH_GRP_USER_R[권한그룹사용자관계] -->
<mapper namespace="com.innobiz.orange.web.wc.dao.WcRescBDao">

	<resultMap id="wcRescBMap" type="WcRescBVo">
		<id property="compId" column="COMP_ID" /><!-- 회사ID -->
		<id property="rescId" column="RESC_ID" /><!-- 리소스ID -->
		<id property="langTypCd" column="LANG_TYP_CD" /><!-- 언어구분코드 -->
		<result property="rescVa" column="RESC_VA" /><!-- 리소스값 -->
	</resultMap>
	<!-- 조회조건 -->
	<sql id="selectWcRescBWhere">
		<where>
			<if test="compId != null and compId != ''"> AND T.COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="rescId != null and rescId != ''"> AND T.RESC_ID = #{rescId, jdbcType=VARCHAR}</if>
			<if test="langTypCd != null and langTypCd != ''"> AND T.LANG_TYP_CD = #{langTypCd, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWcRescB" resultMap="wcRescBMap" parameterType="WcRescBVo" >
		/* com.innobiz.orange.web.wc.dao.WcRescBDao.selectWcRescB */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			T.COMP_ID , T.RESC_ID,	T.LANG_TYP_CD, T.RESC_VA ,
		</trim>
		FROM WC_RESC_B T
		<include refid="selectWcRescBWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.RESC_ID</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWcRescB" resultType="Integer" parameterType="WcRescBVo" >
		/* com.innobiz.orange.web.wc.dao.WcRescBDao.countWcRescB */
		SELECT COUNT(*) CNT
		FROM WC_RESC_B T
		<include refid="selectWcRescBWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWcRescB" parameterType="WcRescBVo" >
		/* com.innobiz.orange.web.wc.dao.WcRescBDao.insertWcRescB */
		INSERT INTO WC_RESC_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			COMP_ID , RESC_ID, LANG_TYP_CD, RESC_VA ,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{compId, jdbcType=VARCHAR},
			#{rescId, jdbcType=VARCHAR},
			#{langTypCd, jdbcType=VARCHAR},
			#{rescVa, jdbcType=NVARCHAR},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWcRescB" parameterType="WcRescBVo" >
		/* com.innobiz.orange.web.wc.dao.WcRescBDao.updateWcRescB */
		UPDATE WC_RESC_B
		<set>
			<if test="rescVa != null"> RESC_VA = #{rescVa, jdbcType=NVARCHAR},</if>
		</set>
		<where>
			<if test="compId != null and compId != ''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="rescId != null and rescId != ''"> AND RESC_ID = #{rescId, jdbcType=VARCHAR}</if>
			<if test="langTypCd != null and langTypCd != ''"> AND LANG_TYP_CD = #{langTypCd, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWcRescB" parameterType="WcRescBVo" >
		/* com.innobiz.orange.web.wc.dao.WcRescBDao.deleteWcRescB */
		DELETE FROM WC_RESC_B
		<where>
			<if test="compId != null and compId != ''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="rescId != null and rescId != ''"> AND RESC_ID = #{rescId, jdbcType=VARCHAR}</if>
			<if test="langTypCd != null and langTypCd != ''"> AND LANG_TYP_CD = #{langTypCd, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>