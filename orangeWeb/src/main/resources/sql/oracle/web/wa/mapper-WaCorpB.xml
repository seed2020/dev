<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WA_CORP_B[법인기본] -->
<mapper namespace="com.innobiz.orange.web.wa.dao.WaCorpBDao">

	<resultMap id="waCorpBMap" type="WaCorpBVo">
		<id property="corpNo" column="CORP_NO" /><!-- 법인번호 -->
		<result property="compId" column="COMP_ID" /><!-- 회사ID -->
		<result property="corpNm" column="CORP_NM" /><!-- 법인명 -->
		<result property="corpRegNo" column="CORP_REG_NO" /><!-- 사업자등록번호 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectWaCorpBWhere">
		<where>
			<if test="corpNo != null and corpNo !=''"> AND T.CORP_NO = #{corpNo, jdbcType=INTEGER}</if>
			<if test="compId != null and compId !=''"> AND T.COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWaCorpB" resultMap="waCorpBMap" parameterType="WaCorpBVo" >
		/* com.innobiz.orange.web.wa.dao.WaCorpBDao.selectWaCorpB */
		<if test="pageNo != null and pageRowCnt != null">
		SELECT * FROM (
		</if>
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			<if test="pageNo != null and pageRowCnt != null">
			* FROM (SELECT N.*, ROWNUM RNUM FROM (SELECT
			</if>
			T.CORP_NO,
			T.COMP_ID,
			T.CORP_NM,
			T.CORP_REG_NO,
		</trim>
		FROM WA_CORP_B T
		<include refid="selectWaCorpBWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.CORP_NM</if>
		<if test="pageNo != null and pageRowCnt != null">
		<![CDATA[) N WHERE ROWNUM <= #{pageNo, jdbcType=NUMERIC} * #{pageRowCnt, jdbcType=NUMERIC}) WHERE RNUM > (#{pageNo, jdbcType=NUMERIC} - 1) * #{pageRowCnt, jdbcType=NUMERIC}]]> ) M
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWaCorpB" resultType="Integer" parameterType="WaCorpBVo" >
		/* com.innobiz.orange.web.wa.dao.WaCorpBDao.countWaCorpB */
		SELECT COUNT(*) CNT
		FROM WA_CORP_B T
		<include refid="selectWaCorpBWhere"/>
	</select>
	
	<!-- 법인 등록 저장 -->
	<insert id="insertWaCorpB" parameterType="WaCorpBVo" >
		/* com.innobiz.orange.web.wa.dao.WaCorpBDao.insertWaCorpB */
		INSERT INTO WA_CORP_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			CORP_NO,
			COMP_ID,
			CORP_NM,
			CORP_REG_NO,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{corpNo, jdbcType=INTEGER},
			#{compId, jdbcType=VARCHAR},
			#{corpNm, jdbcType=NVARCHAR},
			#{corpRegNo, jdbcType=NVARCHAR},			
		</trim>
	</insert>
	
	<!-- 법인 수정 -->
	<update id="updateWaCorpB" parameterType="WaCorpBVo" >
		/* com.innobiz.orange.web.wa.dao.WaCorpBDao.updateWaCorpB */
		UPDATE WA_CORP_B
		<set>
			<if test="corpNm != null"> CORP_NM = #{corpNm, jdbcType=NVARCHAR},</if>
			<if test="corpRegNo != null"> CORP_REG_NO = #{corpRegNo, jdbcType=NVARCHAR},</if>
		</set>
		<where>
			<if test="corpNo != null and corpNo !=''"> AND CORP_NO = #{corpNo, jdbcType=INTEGER}</if>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 법인 삭제 -->
	<delete id="deleteWaCorpB" parameterType="WaCorpBVo" >
		/* com.innobiz.orange.web.wa.dao.WaCorpBDao.deleteWaCorpB */
		DELETE FROM WA_CORP_B
		<where>
			<if test="corpNo != null and corpNo !=''"> AND CORP_NO = #{corpNo, jdbcType=INTEGER}</if>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>