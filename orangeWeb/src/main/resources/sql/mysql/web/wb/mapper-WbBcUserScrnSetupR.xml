<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WB_BC_USER_SCRN_SETUP_R[명함사용자화면설정관계] -->
<mapper namespace="com.innobiz.orange.web.wb.dao.WbBcUserScrnSetupRDao">

	<resultMap id="wbBcUserScrnSetupRMap" type="WbBcUserScrnSetupRVo">
		<id property="compId" column="COMP_ID" /><!-- 회사ID -->
		<id property="regrUid" column="REGR_UID" /><!-- 등록자UID -->
		<result property="inputWndLocCd" column="INPUT_WND_LOC_CD" /><!-- 입력창위치코드 -->
		<result property="lstTypCd" column="LST_TYP_CD" /><!-- 목록구분코드 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectWbBcUserScrnSetupRWhere">
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="regrUid != null and regrUid !=''"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWbBcUserScrnSetupR" resultMap="wbBcUserScrnSetupRMap" parameterType="WbBcUserScrnSetupRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcUserScrnSetupRDao.selectWbBcUserScrnSetupR */
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			COMP_ID , REGR_UID , INPUT_WND_LOC_CD , LST_TYP_CD 
		</trim>
		FROM WB_BC_USER_SCRN_SETUP_R WBUSS_T  
		<include refid="selectWbBcUserScrnSetupRWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY WBUSS_T.REGR_UID ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		LIMIT #{pageStrt, jdbcType=NUMERIC}, #{pageRowCnt, jdbcType=NUMERIC}
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWbBcUserScrnSetupR" resultType="Integer" parameterType="WbBcUserScrnSetupRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcUserScrnSetupRDao.countWbBcUserScrnSetupR */
		SELECT COUNT(*) CNT
		FROM WB_BC_USER_SCRN_SETUP_R WBUSS_T 
		<include refid="selectWbBcUserScrnSetupRWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWbBcUserScrnSetupR" parameterType="WbBcUserScrnSetupRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcUserScrnSetupRDao.insertWbBcUserScrnSetupR */
		INSERT INTO WB_BC_USER_SCRN_SETUP_R
		<trim prefix="(" suffix=")" suffixOverrides=",">
			COMP_ID , REGR_UID , INPUT_WND_LOC_CD , LST_TYP_CD 
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{compId, jdbcType=VARCHAR},#{regrUid, jdbcType=VARCHAR},#{inputWndLocCd, jdbcType=VARCHAR},#{lstTypCd, jdbcType=VARCHAR},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWbBcUserScrnSetupR" parameterType="WbBcUserScrnSetupRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcUserScrnSetupRDao.updateWbBcUserScrnSetupR */
		UPDATE WB_BC_USER_SCRN_SETUP_R
		<set>
			<if test="inputWndLocCd != null"> INPUT_WND_LOC_CD = #{inputWndLocCd, jdbcType=INTEGER},</if>
			<if test="lstTypCd != null"> LST_TYP_CD = #{lstTypCd, jdbcType=VARCHAR},</if>
		</set>
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="regrUid != null and regrUid !=''"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWbBcUserScrnSetupR" parameterType="WbBcUserScrnSetupRVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcUserScrnSetupRDao.deleteWbBcUserScrnSetupR */
		DELETE FROM WB_BC_USER_SCRN_SETUP_R
		<where>
			<if test="compId != null and compId !=''"> AND COMP_ID = #{compId, jdbcType=VARCHAR}</if>
			<if test="regrUid != null and regrUid !=''"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>