<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WB_BC_APNTR_R[명함대리관리자기본] -->
<mapper namespace="com.innobiz.orange.web.wb.dao.WbBcAgntSetupBDao">

	<resultMap id="wbBcAgntSetupBMap" type="WbBcAgntSetupBVo">
		<result property="regrUid" column="REGR_UID" /><!-- 등록자UID -->
		<result property="bcRegrUid" column="BC_REGR_UID" /><!-- 명함등록자UID -->
		<result property="userNm" column="USER_NM" /><!-- 사용자명 -->
	</resultMap>
	
	<!-- 조회조건 -->
	<sql id="selectWbBcAgntSetupBWhere">
		<where>
			<if test="regrUid != null and regrUid !=''"> AND WBAS_T.REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWbBcAgntSetupB" resultMap="wbBcAgntSetupBMap" parameterType="WbBcAgntSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcAgntSetupBDao.selectWbBcAgntSetupB */
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			WBAS_T.REGR_UID , WBAS_T.BC_REGR_UID , T.USER_NM , 
		</trim>
		FROM WB_BC_AGNT_SETUP_B WBAS_T INNER JOIN OR_USER_B T ON WBAS_T.BC_REGR_UID = T.USER_UID
		<include refid="selectWbBcAgntSetupBWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY WBAS_T.BC_REGR_UID ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		LIMIT #{pageStrt, jdbcType=NUMERIC}, #{pageRowCnt, jdbcType=NUMERIC}
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWbBcAgntSetupB" resultType="Integer" parameterType="WbBcAgntSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcAgntSetupBDao.countWbBcAgntSetupB */
		SELECT COUNT(*) CNT
		FROM WB_BC_AGNT_SETUP_B WBAS_T INNER JOIN OR_USER_B T ON WBAS_T.BC_REGR_UID = T.USER_UID
		<include refid="selectWbBcAgntSetupBWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWbBcAgntSetupB" parameterType="WbBcAgntSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcAgntSetupBDao.insertWbBcAgntSetupB */
		INSERT INTO WB_BC_AGNT_SETUP_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			REGR_UID , BC_REGR_UID ,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{regrUid, jdbcType=VARCHAR},#{bcRegrUid, jdbcType=VARCHAR},
		</trim>
	</insert>

	<!-- 삭제 -->
	<delete id="deleteWbBcAgntSetupB" parameterType="WbBcAgntSetupBVo" >
		/* com.innobiz.orange.web.wb.dao.WbBcAgntSetupBDao.deleteWbBcAgntSetupB */
		DELETE FROM WB_BC_AGNT_SETUP_B
		<where>
			<if test="regrUid != null and regrUid !=''"> AND REGR_UID = #{regrUid, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>