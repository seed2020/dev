<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WL_TASK_CONSOL_R[업무일지취합관계] -->
<mapper namespace="com.innobiz.orange.web.wl.dao.WlTaskConsolRDao">

	<resultMap id="wlTaskConsolRMap" type="WlTaskConsolRVo">
		<id property="logNo" column="LOG_NO" /><!-- 일지번호 -->
		<id property="lstNo" column="LST_NO" /><!-- 목록번호 -->
		<result property="sortOrdr" column="SORT_ORDR" /><!-- 정렬순서 -->
	</resultMap>
			
	<sql id="selectWlTaskConsolRWhere">
		<where>
			<if test="logNo != null"> AND T.LOG_NO = #{logNo, jdbcType=INTEGER}</if>
			<if test="lstNo != null"> AND T.LST_NO = #{lstNo, jdbcType=INTEGER}</if>
			<if test="whereSqllet != null and whereSqllet != ''"> ${whereSqllet}</if>
		</where>
	</sql>
	
	<select id="selectWlTaskConsolR" resultMap="wlTaskConsolRMap" parameterType="WlTaskConsolRVo" >
		/* com.innobiz.orange.web.wl.dao.WlTaskConsolRDao.selectWlTaskConsolR */
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			T.LOG_NO,
			T.LST_NO,
			T.SORT_ORDR,
		</trim>
		FROM WL_TASK_CONSOL_R T 
		<include refid="selectWlTaskConsolRWhere" />
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.SORT_ORDR ASC</if>
		<if test="pageNo != null and pageRowCnt != null">
		LIMIT #{pageStrt, jdbcType=NUMERIC}, #{pageRowCnt, jdbcType=NUMERIC}
		</if>
	</select>

	<select id="countWlTaskConsolR" resultType="Integer" parameterType="WlTaskConsolRVo" >
		/* com.innobiz.orange.web.wl.dao.WlTaskConsolRDao.countWlTaskConsolR */
		SELECT COUNT(*) CNT
		FROM WL_TASK_CONSOL_R T
		<include refid="selectWlTaskConsolRWhere" />
	</select>

	<insert id="insertWlTaskConsolR" parameterType="WlTaskConsolRVo">
		/* com.innobiz.orange.web.wl.dao.WlTaskConsolRDao.insertWlTaskConsolR */
		INSERT INTO WL_TASK_CONSOL_R
		<trim prefix="(" suffix=")" suffixOverrides=",">
			LOG_NO,
			LST_NO,
			SORT_ORDR,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides=",">
			#{logNo, jdbcType=INTEGER},
			#{lstNo, jdbcType=INTEGER},
			#{sortOrdr, jdbcType=INTEGER},
		</trim>
	</insert>

	<delete id="deleteWlTaskConsolR" parameterType="WlTaskConsolRVo">
		/* com.innobiz.orange.web.wl.dao.WlTaskConsolRDao.deleteWlTaskConsolR */
		DELETE FROM WL_TASK_CONSOL_R
		<where>
			<if test="logNo != null and logNo !=''"> AND LOG_NO = #{logNo, jdbcType=INTEGER}</if>
			<if test="lstNo != null and lstNo !=''"> AND LST_NO = #{lstNo, jdbcType=INTEGER}</if>
		</where>
	</delete>
	
</mapper>