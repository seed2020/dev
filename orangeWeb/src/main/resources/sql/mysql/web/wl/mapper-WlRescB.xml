<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<!-- TABLE : WL_RESC_B[리소스기본] -->
<mapper namespace="com.innobiz.orange.web.wl.dao.WlRescBDao">

	<resultMap id="wlRescBMap" type="WlRescBVo">
		<id property="rescId" column="RESC_ID" /><!-- 리소스ID -->
		<id property="langTypCd" column="LANG_TYP_CD" /><!-- 언어구분코드 -->
		<result property="rescVa" column="RESC_VA" /><!-- 리소스값 -->
	</resultMap>
	<!-- 조회조건 -->
	<sql id="selectWlRescBWhere">
		<where>
			<if test="rescId != null and rescId != ''"> AND T.RESC_ID = #{rescId, jdbcType=VARCHAR}</if>
			<if test="langTypCd != null and langTypCd != ''"> AND T.LANG_TYP_CD = #{langTypCd, jdbcType=VARCHAR}</if>
		</where>
	</sql>
	
	<!-- 목록조회 -->
	<select id="selectWlRescB" resultMap="wlRescBMap" parameterType="WlRescBVo" >
		/* com.innobiz.orange.web.wl.dao.WlRescBDao.selectWlRescB */
		SELECT
		<trim prefix="" suffix="" suffixOverrides=",">
			T.RESC_ID,
			T.LANG_TYP_CD,
			T.RESC_VA ,
		</trim>
		FROM WL_RESC_B T
		<include refid="selectWlRescBWhere"/>
		<if test="orderBy != null">ORDER BY ${orderBy}</if>
		<if test="orderBy == null">ORDER BY T.RESC_ID</if>
		<if test="pageNo != null and pageRowCnt != null">
		LIMIT #{pageStrt, jdbcType=NUMERIC}, #{pageRowCnt, jdbcType=NUMERIC}
		</if>
	</select>
	
	<!-- 목록조회 건수-->
	<select id="countWlRescB" resultType="Integer" parameterType="WlRescBVo" >
		/* com.innobiz.orange.web.wl.dao.WlRescBDao.countWlRescB */
		SELECT COUNT(*) CNT
		FROM WL_RESC_B T
		<include refid="selectWlRescBWhere"/>
	</select>
	
	<!-- 등록 저장 -->
	<insert id="insertWlRescB" parameterType="WlRescBVo" >
		/* com.innobiz.orange.web.wl.dao.WlRescBDao.insertWlRescB */
		INSERT INTO WL_RESC_B
		<trim prefix="(" suffix=")" suffixOverrides=",">
			RESC_ID,
			LANG_TYP_CD,
			RESC_VA ,
		</trim>
		<trim prefix="VALUES (" suffix=")" suffixOverrides="," >
			#{rescId, jdbcType=VARCHAR},
			#{langTypCd, jdbcType=VARCHAR},
			#{rescVa, jdbcType=VARCHAR},
		</trim>
	</insert>
	
	<!-- 수정 -->
	<update id="updateWlRescB" parameterType="WlRescBVo" >
		/* com.innobiz.orange.web.wl.dao.WlRescBDao.updateWlRescB */
		UPDATE WL_RESC_B
		<set>
			<if test="rescVa != null"> RESC_VA = #{rescVa, jdbcType=VARCHAR},</if>
		</set>
		<where>
			<if test="rescId != null and rescId != ''"> AND RESC_ID = #{rescId, jdbcType=VARCHAR}</if>
			<if test="langTypCd != null and langTypCd != ''"> AND LANG_TYP_CD = #{langTypCd, jdbcType=VARCHAR}</if>
		</where>
	</update>

	<!-- 삭제 -->
	<delete id="deleteWlRescB" parameterType="WlRescBVo" >
		/* com.innobiz.orange.web.wl.dao.WlRescBDao.deleteWlRescB */
		DELETE FROM WL_RESC_B
		<where>
			<if test="rescId != null and rescId != ''"> AND RESC_ID = #{rescId, jdbcType=VARCHAR}</if>
			<if test="langTypCd != null and langTypCd != ''"> AND LANG_TYP_CD = #{langTypCd, jdbcType=VARCHAR}</if>
		</where>
	</delete>

</mapper>